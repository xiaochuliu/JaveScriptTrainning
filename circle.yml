machine:
  environment:
    DOWNSTREAM: remotebeta/autodesk-web-reviewer
dependencies:
  pre:
    - npm install -g reactorcore/problemify
    - git clone git@github.com:reactorcore/utils.scripts.git ../scripts
test:
  override:
    - echo "tests not run"
deployment:
  master:
    branch: master
    commands:
      - ../scripts/circle-ci/deploy/publish-to-remotebeta.sh
